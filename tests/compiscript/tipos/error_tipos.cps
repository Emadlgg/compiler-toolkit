// Error: operacion aritmetica con string
let x: integer = 10;
let y: string = "hola";
let mal = x + y;

// Error: operacion logica con integer
let z = 5 && true;

// Error: negacion sobre integer
let w = !10;
